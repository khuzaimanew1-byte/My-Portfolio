Deeply analyze the current network implementation before changing visuals.

The problem is NOT only styling.
The architecture of the line system itself is wrong.

Current issue:
The lines are behaving like static SVG paths with glow.
Because of this:

* animation feels fake
* pulses feel detached
* curves feel frozen
* movement has no physical continuity
* signal flow is not believable

Rebuild the system architecture.

IMPORTANT:
Do NOT animate the SVG path itself.

Instead:

* keep the bezier path stable
* animate a signal entity traveling ALONG the path
* use path progress logic
* use path length interpolation
* use directional signal propagation

Correct architecture:

1. Calculate live bezier path between nodes
2. Store actual path length
3. Create moving signal particle traveling along path progress
4. Signal leaves fading trail behind
5. Receiving node activates on arrival
6. Then next transmission begins

Do NOT:

* fade entire line on/off
* scale line glow
* animate opacity randomly
* animate stroke width globally
* animate all nodes together

The animation must come from:
MOVING ENERGY
not from:
STATIC LINE EFFECTS

Technical direction:

* use SVG path length
* use getPointAtLength()
* animate signal progress from 0 → pathLength
* derive signal position from actual curve coordinates
* update glow/trail based on signal position
* use requestAnimationFrame timing
* derive propagation from node chain state

Line rendering:

* inactive line = extremely subtle low-opacity structure
* active signal = moving illuminated particle
* illuminated trail follows behind signal
* trail fades progressively
* line only bright near active transmission zone

Node behavior:

* source node charges first
* emits transmission
* receiving node reacts ON signal arrival
* then becomes next source

This is critical:
The network should feel like:
real signal propagation through infrastructure.

NOT:
decorative hover lines.

Curve quality:

* bezier curves must be recalculated dynamically
* curves adapt to floating icon movement
* control points derived from spatial relationship
* no hardcoded arc shapes

Spatial rules:

* avoid icon overlap
* avoid unnatural symmetry
* preserve cinematic composition

Motion rules:

* transmission speed based on path distance
* slight easing during acceleration/deceleration
* no constant robotic speed
* no infinite loop motion

Visual tone:

* restrained
* premium
* cinematic
* intelligent
* operational

Final result should feel like:
a real-time intelligent signal infrastructure where flowing energy travels physically through a living cinematic technology network.
